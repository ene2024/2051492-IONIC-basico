import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '2051492-IONIC-basico',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
